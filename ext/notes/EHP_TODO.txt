EHP open work — July 7 2026 session end
========================================
Companion docs: CHANGES_2026-07-07.md (every change today + revert paths),
SESSION_HANDOFF.md (architecture + July 7 section). Committed at cbad4f56,
pushed to remote branch `ehp-chart`.

1. d4(3,32,9) SOUNDNESS — RESOLVED July 7 (evening)
   Root cause: cascade_resolve re-turned only stable-REP degrees, never their
   stable copies -> inconsistent stable fold -> wrong E-naturality/h_i-Leibniz
   constraints falsely forced the d4 to 0. Fresh chains DERIVE d4(3,32,9)=1
   from the user's other 8 adds (verified incl. exact saved state).
   FIXED in ehp_chart.rs: expand_stable_fold in the cascade + stable-fold
   guardrail (warn + self-heal full re-turn on violation) + diag_stable_fold
   regression harness. If UNSAT ever recurs live: run diag_unsat_cert
   scenario D BEFORE undoing (the corrupted state dies with the undo).
   Remaining (optional): the "UNSAT quarantine" (re-verify INCONSISTENT
   against a fresh chain before reporting) — TODO comment at the
   unsat_reason assignment.
   [superseded original text follows]
   User asserts this d4 is nontrivial; adding it made E4 INCONSISTENT in the
   live session. Findings so far: on CLEAN page chains at t=70 the value is
   SAT both alone and with the other 8 batch adds — the contradiction needs
   the session's accumulated known-diffs state. Final scenario report pending.
   NEXT STEP (deterministic reproduction):
     - user runs `save` in the REPL session that hit the contradiction
       (state minus the undone d4 is fine — the diag re-adds it)
     - run: EHP_MAX_T=70 EHP_MAX_R=5 \
            DIAG_KNOWN_DIFFS=<saved-file> \
            cargo run -p ehp-server --release --example diag_unsat_cert
     - the diag prints the UNSAT certificate: the exact constraints whose sum
       is 1=0, each with provenance (naturality square / Leibniz pair /
       known-diff + degrees + matrices) — a topologist can eyeball which
       relation is bogus, or which recorded value is wrong.

2. Exclusion refinement — final verdict  [IN PROGRESS]
   Target-only-excluded degrees now get d_r variables + source-side Leibniz
   pairs (motivated by S43 (34,4) d3 and S38 (18,4) d4 — both counterfactually
   forced = 0, matching the user's h0/h1 Leibniz arguments).
   KILL-SWITCH: EHP_RELAX_TARGET_EXCLUDE=0 restores old behavior exactly
   (parity-validated byte-identical). Committed default-ON.
   PENDING: the t=70 startup timing comparison (must be within ~10%; if the
   runs were contended, redo back-to-back solo). If the verdict is bad:
   either fix the hot path or flip the default to OFF — one-line change.
   THEN: run diag_leibniz_var on BOTH user cases with feature on:
     DIAG_N=43 DIAG_S=34 DIAG_F=4 DIAG_R=3  → expect DETERMINED = 0
     DIAG_N=38 DIAG_S=18 DIAG_F=4 DIAG_R=4  → expect DETERMINED = 0 if the
       exclusion was target-only; if still excluded, the exclusion is
       carried-forward — extend the refinement to classify
       carried-forward-from-target-only chains (follow-up work).

3. User validation of the sweep speedup at full scale  [USER]
   sweep 3 at EHP_MAX_T=100 in the REPL (original goal: under 20 min; t=70 was
   ~4 min for 2492 trials). Also eyeball forced values / charts.
   Also still unmeasured: one clean t=70 sweep timing with the final shipped
   lib (last clean number 611s predates the partners-iterator fix; t=50=9.9s).

4. Startup performance round 2  [NOT STARTED]
   t=90-through-E5 startup was ~30 min; build_pairs removal (~13%) landed.
   Remaining, in profile order:
   - build_page_from_turned (~14%): profile a startup-only run
     (diag_sweep with DIAG_MIN_STEM=100000 = pure startup), then batch
     ProductTable block construction / parallelize serial assembly.
   - ~36% of cores idle during serial phases — parallelize constraint
     generation and CSV load if profile confirms.
   - USER'S IDEA (recorded in CHANGES spec §3): gate the Leibniz pair
     enumeration with a precomputed nonzero-product pair index (what
     `pairs` was meant to be — build_pairs removal did NOT cause extra
     pair-checking, the enumeration never read it, but an index CONSUMER
     would cut the ~95k-candidate scans dramatically).
   - CAUTION (memory: ehp-perf-benchmark-at-scale): any change here must be
     benchmarked on a real t>=70 workload; Leibniz generation is fail-fast
     dominated — never add work above its skip() exits. A hoisting attempt
     was reverted today for exactly this.

5. Leibniz optimization, correctly this time  [NOT STARTED]
   The reverted rewrite's arithmetic wins were real (d1*t1+d2*t2 vs
   d1*d2*(t1+t2)) — reimplement with fully-lazy memoization at the original
   construction sites (guidance in the revert agent's report + memory note).

6. Git: reconcile secondary-steenrod-changes divergence  [USER DECISION]
   Remote has 5 rewritten (rebased/amended) duplicates of local commits.
   Local work is safe on branch ehp-chart. Needs a clean working tree and a
   decision (rebase vs merge; which side wins) before pushing the main branch
   again. Do NOT git checkout/stash the dirty tree without explicit sign-off.

7. Shared SeqSee repo changes are NOT in this git repo  [NOTE]
   ~/seqsee/seqsee_new/template.html.jinja (stem titles E_r(pi^k)) and
   ~/SeqSee/seqsee/template_sidebyside.html.jinja (image-mode differential
   fix) live in the shared SeqSee checkout — commit/distribute separately if
   the collaborator needs matching charts.

8. Smaller follow-ups
   - Split-screen map views are not auto-regenerated after mutations (known
     residual staleness; sphere/stem charts are). Extend regen_affected_charts
     if it bothers the user.
   - interpage try / sweep on other pages: exercise the new unsat_reason and
     auto-bisect paths at scale.
   - [DONE July 7] EHP_OUTSIDE_DIFFS=<dir> loads the outside_diffs CSVs
     (r,n,s,f,row,col,value; all csv files in the dir) into known diffs at
     startup. NOTE (user): this is NOT where Python knows d2(36,35,2)=0
     from — it is simply additional useful input data.
